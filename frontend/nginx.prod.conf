# ============================================================
# Nginx Production Config — learnzh.website + SSL
# ============================================================

# ── HTTP → Redirect to HTTPS ──
server {
    listen 80;
    server_name learnzh.website www.learnzh.website;

    # Certbot challenge (cần cho việc xác thực SSL)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Chuyển hướng tất cả HTTP sang HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# ── HTTPS — Main server ──
server {
    listen 443 ssl;
    http2 on;
    server_name learnzh.website www.learnzh.website;

    # ── SSL Certificate (Let's Encrypt) ──
    ssl_certificate /etc/letsencrypt/live/learnzh.website/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/learnzh.website/privkey.pem;

    # ── SSL Security Settings ──
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # ── Security Headers ──
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    root /usr/share/nginx/html;
    index index.html;

    # ── Upload size — cho phép upload file lớn ──
    client_max_body_size 100m;

    # ── Gzip ──
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;

    # ── Angular SPA — fallback to index.html ──
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── Cache static assets 1 year ──
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ── Reverse proxy: /api/ → backend ──
    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
        proxy_send_timeout 120s;

        # Proxy buffering cho response lớn
        proxy_buffering on;
        proxy_buffer_size 16k;
        proxy_buffers 4 32k;
    }

    # ── Reverse proxy: /audio/ → backend static files ──
    location /audio/ {
        proxy_pass http://backend:8080/audio/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        # Cache audio files ở nginx
        proxy_cache_valid 200 1d;
        expires 1d;
        add_header Cache-Control "public";
    }

    # ── Reverse proxy: SignalR Hub (WebSocket) ──
    location /hubs/ {
        proxy_pass http://backend:8080/hubs/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support — bắt buộc cho SignalR
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Reverse proxy: /swagger → backend (chỉ dùng lúc debug) ──
    location /swagger {
        proxy_pass http://backend:8080/swagger;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
